# Specs

Feature/change specifications for spec-driven development.

Create one with `/create-spec`, implement it with `/execute-spec specs/NNN-name.md`.
See `.claude/skills/create-spec/SKILL.md` for the format. Numbering is zero-padded and incrementing (001, 002, ...).

Completed specs can be moved to `spec_archive/`.
